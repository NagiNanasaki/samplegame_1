# samplegame_1 作業メモ
最終更新: 2026-02-27

---

## プロジェクト基本情報
- **場所**: `C:\Users\user\Desktop\samplegame_1\`
- **エンジン**: ティラノスクリプト
- **解像度**: 1280×720

---

## 今日やったこと

### 1. scene1.ks - 星スチルから教会に戻る際のフラッシュ修正（済み）
- **問題**: layer1の星画像をクリアしてから`[mask_rule]`を呼んでいたため、一瞬ズーム状態のbase層の星が見えた
- **修正**: iscriptを`[mask_rule]`の**後**に移動（scene1.ks 177〜186行付近）

### 2. scene1.ks → title.ks への遷移「ドン」修正（済み）
- **問題**: scene1終了時にフェードアウトなし＋title.ksが`time=100`でほぼ瞬間表示
- **修正**:
  - `scene1.ks` 末尾: `[mask time="800" color="0x000000"]` を追加（BGM/SEフェードと並行）
  - `title.ks`: `@bg time=0` + `@mask_off time="1000"` に変更

### 3. title.ks - タイトル画面リニューアル（済み）

#### 使用ファイル
| ファイル | 元パス | 配置先 |
|---|---|---|
| 背景 | `data/image/title_menu.png` | `data/bgimage/title_menu.png` にコピー済み |
| ロゴ | `C:\Users\user\Desktop\tail-tale\Perseid\data\image\title\title_logo.png` | `data/image/title/title_logo.png` にコピー済み |

#### ボタン構成（CSSテキストボタン）
| ID | 表示 | 処理 |
|---|---|---|
| `#tbtn_newgame` | New Game / はじめから | `scene0.ks` へジャンプ |
| `#tbtn_continue` | Continue / つづきから | `TYRANO.kag.menu.loadgame()` |
| `#tbtn_config` | Config / 環境設定 | `sleepgame` 経由で `config.ks`（`[awakegame]`で復帰） |
| `#tbtn_extra` | Extra / エクストラ | `cg.ks` へジャンプ |
| `#tbtn_exit` | Exit / ゲーム終了 | `nw.App.quit()` |

#### 実装方式
- `[iscript]` で `#title_ui` div を `.tyrano_base` に append
- ロゴは `right:50px; top:120px; width:500px`（右上寄り）
- ボタンは `position:absolute; bottom:45px; display:flex; justify-content:space-around`

---

## 4. title.ks - タイトル画面アニメーション追加（済み）

### 実装内容（anime.js v3.2.1 使用 / `tyrano/libs/anime.min.js`）

| アニメーション | 内容 |
|---|---|
| ロゴ フェードイン | delay:300ms / duration:1200ms / 上から-30px滑り込み + opacity 0→1 |
| ボタン staggered 出現 | delay:900ms〜（130msずつ時間差） / duration:900ms / 下から+20px |
| 背景 光の揺らぎ | `#title_shimmer` div + CSS keyframe `tsw-shimmer` / 6s 周期で opacity 0.6↔1 |

### 初期状態の隠し方
- `#title_logo` の CSS に `opacity:0` を追加
- `.title-btn` の CSS に `opacity:0` を追加
- anime.js が `[0, 1]` 形式で from→to を管理

---

## 次回やること

特になし（タイトル画面は一通り完成）。必要なら以下を検討：
- ボタンのクリック時アニメーション（現在は瞬間遷移）
- シマー光の位置・強度の調整（現在: 右上70%/20%、強度0.18）

---

## 既知の注意点・将来の懸念

### title.ks
- `Continue` ボタン: `TYRANO.kag.menu.loadgame()` はTSバージョン依存。効かない場合は `TYRANO.kag.menu.loadMenu()` や `TYRANO.kag.ftag.startTag('load', {})` を試す
- CG から戻ると `title.ks` が先頭から再実行される（BGM が再スタートする可能性あり）
- `*gamestart` ラベルは現在未使用（旧ボタン用の名残）

### scene1.ks
- star.png スチルのカメラパン（9秒）は非同期。タイミングはセリフ量で変わる点に注意
- `reset_camera layer="base" time="1"` は `title.ks` 先頭でも呼ばれており二重になっているが問題なし

---

## ファイル構成（タイトル関連）
```
data/
├── bgimage/
│   └── title_menu.png   ← 教会背景（新規追加）
├── image/
│   └── title/
│       ├── title_logo.png  ← Perseid.ロゴ（新規追加・透過PNG）
│       └── button_*.png    ← 旧ボタン（未使用・残存）
└── scenario/
    ├── title.ks    ← 今日リニューアル
    └── scene1.ks   ← 今日バグ修正2件
```
