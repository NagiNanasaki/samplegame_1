# samplegame_1 作業メモ
最終更新: 2026-02-28

---

## プロジェクト基本情報
- **場所**: `C:\Users\user\Desktop\samplegame_1\`
- **エンジン**: ティラノスクリプト
- **解像度**: 1280×720

---

## 今日やったこと（2026-02-28）

### 1. scene1.ks → title.ks でシステムボタンが残る問題（済み）
- **問題**: scene1終了後にタイトルへ戻るとSAVEなどのシステムボタンが表示されたまま
- **修正**: scene1.ks 末尾の `[layopt layer="message0" visible="false"]` の直後に `[clearfix name="role_button"]` を追加
- **教訓**: `[mask]` で黒画面の状態でも fixlayer は生きている。jump 前に明示的に `[clearfix]` すること

### 2. Config/Extraからタイトルに戻るとキャラ・ボタンが復帰しない問題（済み）
- **根本原因**: `[awakegame]` がTyranoScriptのレイヤーを復元する際に、iscriptで作成した `#title_ui` DOM要素を消してしまう
- **誤った修正**: プラグインフォルダ（`data/others/plugin/thema_nagi_1/config.ks`）を編集していたが、実際に使われていたのは `data/scenario/config.ks` と `data/scenario/cg.ks` だった
- **正しい修正**:
  1. `data/scenario/config.ks` `*backtitle`: `[awakegame]` → `[jump storage="title.ks"]`
  2. `data/scenario/cg.ks` `*backtitle`: `[awakegame]` → `[clearstack]` + `[jump storage="title.ks"]`
  3. `title.ks` Config/Extra ボタンハンドラ: `sleepgame` → `jump`
- **教訓**:
  - **`[awakegame]` はiscriptで追加したDOM要素を消す**。iscript製のUIと sleepgame/awakegame は相性が悪い
  - `[jump]` で戻る方式に変えると title.ks が頭から再実行される → アニメーション再生・BGM再起動の副作用が出る（↓で対処）
  - TSのシナリオフォルダ（`data/scenario/`）と プラグインフォルダ（`data/others/plugin/`）に同名ファイルが存在することがある。どちらが実際に呼ばれているか必ず確認する

### 3. Config/Extra復帰時にタイトル演出が再生される・BGMが再起動する問題（済み）
- **問題**: `[jump storage="title.ks"]` で戻るため title.ks が頭から再実行 → アニメーションフル再生・`[playbgm]` が再実行
- **修正**:
  - Config/Extra ボタンハンドラで `tf._title_skip_anim = true` をセットしてからジャンプ
  - title.ks のiscript先頭で `var _skipAnim = !!(tf._title_skip_anim); tf._title_skip_anim = false;` で読み取り・リセット
  - `_skipAnim` が true のとき: 霞・キャラ・ロゴ・ボタン・orb を即座に最終状態に設定（animate不使用）、fade_overlay のみ短縮（600ms）で表示
  - `[playbgm]` を `[if exp="!tf._title_skip_anim"]...[endif]` で囲んで復帰時はスキップ
- **教訓**:
  - `tf` 変数は `[jump]` をまたいで保持される（`loadGameData` が呼ばれない限りクリアされない）
  - `[playbgm]` の時点では `tf._title_skip_anim` はまだ `true`（リセットは後の iscript）なので `[if]` で直接参照できる

---

## 前回やったこと（2026-02-27）

### 1. scene1.ks - 星スチルから教会に戻る際のフラッシュ修正（済み）
### 2. scene1.ks → title.ks への遷移「ドン」修正（済み）
### 3. title.ks - タイトル画面リニューアル（済み）
### 4. title.ks - タイトル画面アニメーション追加（済み）

---

## 今日やったこと（2026-03-04）

### Config → タイトル → New Game で config の画像が残るバグ（済み）
- **症状**: Config から戻って New Game すると、scene1 にconfigのボタン（インジケーター画像）が残って見える
- **原因**: config.ks で `layer="0"` に置いた BGM・SE・テキスト速度・オート速度・スキップのインジケーター画像を `*backtitle` でクリアしていなかった。`[clearfix]` は fixlayer のボタンのみクリアし layer="0" の `[image]` は対象外
- **修正**: `*backtitle` の `[clearfix]` の前に以下を追加
  ```ks
  [free layer="0" name="bgmvol" time="0" wait="true"]
  [free layer="0" name="sevol"  time="0" wait="true"]
  [free layer="0" name="ch"     time="0" wait="true"]
  [free layer="0" name="auto"   time="0" wait="true"]
  [free layer="0" name="skip"   time="0" wait="true"]
  ```
- **教訓**: `[clearfix]` は fixlayer ボタンのみ対象。`[image layer="0" ...]` で置いた画像は `[free layer="0" name="..."]` で個別に消す必要がある（`name` は必須）

### スチル中のSAVEサムネイル・操作不可問題（済み）
- **症状1**: スチル後にSAVEすると星画像がサムネイルに残る
- **症状2**: スチル中にシステムボタン（SAVE等）が反応しない
- **症状3**: スチル後SAVE→進む→Load→SAVEするとまた星が出る

- **原因1（症状2）**: `$('.fixlayer').css('pointer-events','none')` でシステムボタンごとブロックしていた
- **修正1**: fixlayer の `pointer-events:none` を削除。opacity:0 フェードは維持し、視覚的には非表示だがクリック可能に

- **原因2（症状1・3）**: `[bg storage="church.jpg" time=0]` をマスク中に呼ぶと TyranoScript の内部ステートが更新されないことがある
- **修正2**: `[mask_off]` 完了後に `[bg storage="church.jpg" time=0]` を再度呼んでステートを確定

- **原因3（症状1）**: CSS で `background-image:""` は環境によって旧値が残ることがある。また `opacity:""` でデフォルト1に戻り layer="1" の star.png が見えたまま
- **修正3**: `background-image:"none"` に変更、`opacity:"0"` を明示。`[layopt layer="1" visible="false"]` で TyranoScript 側にも非表示を通知

- **教訓**:
  - マスク中の `[bg]` はステートが確定しない→マスク解除後に再呼び出しで確定させる
  - CSS `background-image:""` は `"none"` を使うべき
  - fixlayer 全体に `pointer-events:none` するとシステムボタンも封じる→フェードだけして pointer-events は維持

## 今日やったこと（2026-03-04 続き）

### qsave/qload後にキャラ名表示が消えるバグ（済み）
- **症状**: qsave→qloadするとキャラ名（`chara_name_area` ptext）が空になる
- **原因**: TyranoScriptのHTML save/loadはptext要素の**テキスト内容**を保存・復元しない。ptextのDOM要素自体は復元されるが中身が空になる
- **調査過程**:
  - `stat.last_chara_name` に保存 → `clearTmpVariable` でクリアされていた
  - `f._last_chara_name` に変更 → `pm.val` を使っていたが実際のパラメータは `pm.name`
  - `# 主人公` → `[chara_ptext name="主人公" face=""]` に変換される（`val` ではなく `name`）
- **修正**:
  1. `title.ks` の先頭iscriptに `chara_ptext` タグのフックを追加（`_hooked` フラグで二重登録防止）
     - フック内で `f._last_chara_name = pm.name` を保存
     - 元の関数は `TYRANO.kag.tag.chara_ptext._origStart` に退避（evalスコープ問題を回避）
  2. `make.ks` にフックの再設定（ロード後のゲーム進行に備えて）＋setTimeout(100ms)でptextを `f._last_chara_name` から復元
- **教訓**:
  - `# キャラ名` KS記法 → `[chara_ptext name="キャラ名" face=""]`（パラメータは `name`、`val` ではない）
  - `stat` に直接追加したプロパティは `clearTmpVariable` で消える → `f` 変数（`stat.f`）を使うこと
  - `[iscript]` の `var` はそのeval実行コンテキスト内でしか有効でない → タグオブジェクトのプロパティ（`TYRANO.kag.tag.xxx._origStart`）に退避することで別スコープからアクセス可能

---

## コードレビュー対応（2026-03-04）

### config.ks switch文簡略化（済み）
- BGM/SE音量: `Math.round(vol / 10)` に置き換え
- テキスト速度/オート速度: 配列 `.indexOf()` に置き換え（57行削減）

### scene1.ks `[transparent_frame]0"]` の扱い（済み）
- 元々構文エラーでパーサーにスキップされていたタグ
- 「修正」して実行されるようになったら opacity:0 のままメッセージが消えた
- **結論**: そのタグ呼び出し自体が不要だったため削除
- **教訓**: 壊れたコードを修正するときは「エラーのせいでスキップされていた」可能性を先に検討する

### title.ks デッドコード削除（済み）
- `*do_newgame` / `*gamestart` / `*do_load` / `*do_config` / `*do_exit` を削除
- 現在はJSハンドラで直接遷移しているため全て到達不能だった

### chara_ptext フック重複について（意図的に保持）
- title.ks と make.ks 両方にフックコードあり
- title.ks → New Game ルートで必要、make.ks → ロードルートで必要
- `_hooked` フラグで二重設定は防いでいる。削除は不可

### Config.tjs リリース設定の修正（済み）
- `projectID`: `tyranoproject` → `nagi_sample`
- `debugMenu.visible`: `true` → `false`
- `game_version`: `0.0` → `1.0`
- `System.title`: サンプル（現状維持）

### コードレビュー全体完了（済み）
- `config.ks`: switch文 → Math.round / indexOf に簡略化（57行削減）
- `scene1.ks`: 構文エラーの `[transparent_frame]0"]` を削除
- `title.ks`: 到達不能な `*do_newgame` / `*gamestart` / `*do_load` / `*do_config` / `*do_exit` を削除
- `Config.tjs`: `projectID=nagi_sample` / `debugMenu.visible=false` / `game_version=1.0`
- `scene0.ks` / `cg.ks` / `replay.ks` / `first.ks` / `make.ks`: 問題なし・現状維持

---

## 2周目でシーンチェンジが動かない（解決済み）
- **症状**: 2周目（NewGame2回目）でシーンチェンジ後、背景が変わらない
- **根本原因**: `[jump]` でシーンを繰り返すたびに `.base_fore` 要素がDOMに蓄積される。`bg_rule.js` が常に `base_fore[0]`（古い要素）を参照していたため、2周目以降は非表示の `base_fore` に対して演出・背景変更していた
- **修正**: `bg_rule.js` の `start` / `getPrevImageSrc` / `changeBaseLayer` の3箇所を `base_fore[0]` → `base_fore[length-1]`（最後の要素）に変更
- **教訓**: `bg_rule.js` のような外部プラグインは `base_fore[0]` を固定参照していることがある。`[jump]` でのループがある場合は `base_fore` が複数存在することに注意

## 残課題

### シーンチェンジ後にキャラ・ウィンドウが表示されない（解決済み）
- **症状**: シーンチェンジ演出（廊下→教会）の後、`[chara_show]`と`[layopt visible=true]`が実行されない
- **根本原因**: `[wait]` タグが `[bg]`/`[mask]` 系タグの後で止まる（`weaklyStop`/`stronglyStop` の干渉と推測。詳細未解明）
- **解決策**: `[bg_rule]` プラグインを導入。`[bg_rule]` はルール画像を使ったシーンチェンジを内部でブロッキング処理するため、`[wait]` の問題を回避できる
- **最終コード**:
  ```ks
  ;UIとキャラをフェードアウト
  [iscript]
  $(".message0_fore").children().stop(true,true).animate({opacity:0}, 2000);
  $(".fixlayer").stop(true,true).animate({opacity:0}, 2000);
  [endscript]
  [wait time="500"]
  [chara_hide name="tensi" time="300"]
  [layopt layer="message0" visible="false"]

  ;bg_ruleプラグインでルール画像シーンチェンジ
  [bg_rule storage="rouka.jpg" rule="001.png"]

  ;UIとキャラをフェードイン
  [layopt layer="message0" visible="true"]
  [iscript]
  $(".message0_fore").children().stop(true, true).animate({opacity: 1}, 1000);
  $(".fixlayer").stop(true, true).animate({opacity: 1}, 1000);
  [endscript]
  [wait time="1000"]
  [chara_show name="tensi" top=50 time=400]
  ```
- **教訓**: `[mask]`/`[bg time=X]`の後の`[wait]`は止まる。シーンチェンジには`[bg_rule]`プラグインを使うと`[wait]`が正常動作する

### Bug #3: スキップ操作後にスチルの白暗転が明けない（解決済み）
- 解決策はMEMORY.mdの「白暗転スキップ後に明けない」参照

---

## 既知の注意点・将来の懸念

### title.ks
- `Continue` ボタン: `TYRANO.kag.menu.loadgame()` はTSバージョン依存。効かない場合は `TYRANO.kag.menu.loadMenu()` や `TYRANO.kag.ftag.startTag('load', {})` を試す
- `*gamestart` ラベルは現在未使用（旧ボタン用の名残）
- Config/Extra は `sleepgame` でなく `jump` で遷移。`data/scenario/config.ks` と `cg.ks` が使われている（プラグイン版ではない）

### scene1.ks
- star.png スチルのカメラパン（9秒）は非同期。タイミングはセリフ量で変わる点に注意

---

## ファイル構成（タイトル関連）
```
data/
├── bgimage/
│   └── title_menu.png   ← 教会背景
├── image/
│   └── title/
│       └── title_logo.png  ← Perseid.ロゴ
└── scenario/
    ├── title.ks    ← タイトル画面（anime.js UI、sleepgameなし）
    ├── config.ks   ← コンフィグ画面（data/scenario/にある本体）
    ├── cg.ks       ← CGモード（data/scenario/にある本体）
    └── scene1.ks   ← メインシナリオ
```
